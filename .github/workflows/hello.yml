name: Simple NodeJs
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    Container: node:14
    steps:
      - run: node --version
      - run: npm -version
      - uses: actions/checkout@v3
      - run: npm install
      - run: node app.js
